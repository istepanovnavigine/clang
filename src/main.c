#include <stdio.h>

int setbits(int x, int p, int n, int y);

int main() {
    int x = 0, p = 0, n = 0, y = 0;
    scanf("%d\n", x);
    scanf("%d\n", p); 
    scanf("%d\n", n);
    scanf("%d\n", y);


    printf("%d\n", x);

    return 0;
}

int setbits(int x, int p, int n, int y) {
    
}

